--index-url https://download.pytorch.org/whl/cu128 # for torch/torchaudio
--extra-index-url https://pypi.org/simple # default for everything else

torch==2.8 # torch 2.8 with CUDA 12.8 (pyannote 4.0 does not work with torch 2.9)
torchaudio==2.8 
AdvancedHTMLParser
appdirs
cpufeature
customtkinter
CTkToolTip
faster-whisper
Pillow
pyannote.audio>=4.0
pyinstaller==6.14.1 
# The bootloader in the most recent version of pyinstaller is often falsely detected as malware
# by anti virus software, so, it's safer to use a version that has been released a few months ago. 
python-i18n
PyYAML